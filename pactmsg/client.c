#include "client.h"
#include "debug.h"

/*
pact_client_t* pact_client_create(pact_server_data_t* serverdata)
{
	if (!serverdata)
		return 0;

	pact_client_t* client = malloc(sizeof(pact_client_t));

	if (!client)
		return 0;

	memset(client, 0, sizeof(pact_client_t));

	client->serverdata = serverdata;

	return client;
}
*/
/*
pact_server_data_t* _pact_server_data_create(char* hostname, unsigned short int port, char* pass)
{
	if (!hostname)
		return 0;
	pact_server_data_t* serverdata = malloc(sizeof(pact_server_data_t));

	if (!serverdata)
		return 0;

	memset(serverdata, 0, sizeof(pact_server_data_t));

	serverdata->hostname = hostname;
	serverdata->port = port;
	serverdata->pass = pass;
}
*/